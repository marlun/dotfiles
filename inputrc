# When using TAB completion if there are more then one match
# show them all instantly
set show-all-if-ambiguous on

# When using TAB completion ignore the case of the file. This
# means 'doc<TAB>' can complete into 'Documents'
set completion-ignore-case on

# Add / after symlinked directories on TAB completion
set mark-symlinked-directories on

# Didn't get this to work, not sure if I understand
set skip-completed-text on

# Keybindings
Control-j: menu-complete
Control-k: menu-complete-backward
